: '
  iterate over all declare assocative arrays ("objects")
  and if they have no "references" (their own names/keys in other
  variables, including other associative arrays and arrays), delete
  them.
  but maybe only if the have a generated name matching some form
  corresponding to our allocator.
'
